export class Ingredient {
  //short hand for ts so you don't have to provide the format beforehand. Adding the visibility it key.
  constructor(public name:string, public measurement: string,  public amount: number) {

  }
}
